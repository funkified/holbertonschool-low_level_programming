#include <stdio.h>

/**
 * main - print all number in base 10 starting from 0
 *
 * Return: Always 0 if success
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
