#include "main.h"

/**
 * _abs - computes the absolute value of an interger
 *@n: tested interger
 *Return: absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}

